function PageTitle({ title }) {
    return (
        <div className="mt-28 mb-9">
          <h1 className="text-primary text-[21px] font-bold text-center">{title}</h1>
        </div>
    );
};

export default PageTitle;